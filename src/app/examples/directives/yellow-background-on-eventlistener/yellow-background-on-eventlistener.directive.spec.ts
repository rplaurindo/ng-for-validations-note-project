import { YellowBackgroundOnEventlistenerDirective } from './yellow-background-on-eventlistener.directive';

describe('YellowBackgroundOneventlistenerDirective', () => {
  it('should create an instance', () => {
    const directive = new YellowBackgroundOnEventlistenerDirective();
    expect(directive).toBeTruthy();
  });
});
